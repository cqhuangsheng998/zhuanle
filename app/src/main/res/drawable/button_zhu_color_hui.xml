<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- 填充的颜色 -->
    <solid android:color="@color/zhu_weight1" />
    <!--设置渐变-->

    <!--<gradient android:startColor="@color/color_theme_10"
        android:endColor="@color/zhu_weight1"
        android:angle="180"/>-->
    <!--angle控制渐变的方向-->
    <!-- 设置按钮的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="@dimen/dp_5" />
    <!-- padding：Button里面的文字与Button边界的间隔 -->
    <padding
        android:left="@dimen/dp_10"
        android:top="@dimen/dp_6"
        android:right="@dimen/dp_10"
        android:bottom="@dimen/dp_6"
        />
</shape>